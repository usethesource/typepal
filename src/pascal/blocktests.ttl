test Asg1 [[ var n : integer; begin n := n end ]]

test Asg2 [[ const k = 10; var n : integer; begin n := k end ]]

test Asg3 [[ const k = 10; var x : real; begin x := k end ]]

test Asg4 [[ const k = 10; var x : real; begin x := k + 1.5 end ]]

test Asg5 [[ const k = 10; var x : real; begin x := k + 1.5 * x end ]]

test Asg6 [[ var n : integer; begin n := m end ]] expect {"Undefined `m`"}

test Asg7 [[ var n : integer; begin n := 'abc' end ]] expect {"Incorrect assignment, expected subtype of `integer`, found `string`"}

test If1 [[ var n : integer; begin if true then n := 1 end ]]

test If2 [[ var n : integer; begin if n < 0 then n := 1 end ]]

test If3 [[ var n : integer; begin if 13 then n := 1 end ]] expect {"Incorrect condition, expected `Boolean`, found `integer`"}